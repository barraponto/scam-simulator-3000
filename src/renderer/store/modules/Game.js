const state = {
  level: 1,
  mission: false,
};

const mutations = {
};

const actions = {
};

const getters = {
  mission: state => state.mission,
  level: state => state.level,
};

export default {
  state,
  mutations,
  actions,
  getters,
};
